#!/bin/sh
#URL="https://alerts.weather.gov/cap/fl.php?x=0"

#ENTS=$(cat us.xml | grep -o '<entry>[^<]*' | grep -o "[^>]*$")
#SUMS=$(cat us.xml | grep -o '<summary>[^<]*' | grep -o "[^>]*$")

#echo $ENTS

